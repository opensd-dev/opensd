# OpenSD web GUI requirements

Requirements for the React + Vite + React Flow viewer in `opensd-web`.

| Document | Description |
|----------|-------------|
| [opensd-web-gui.md](opensd-web-gui.md) | Canonical GUI requirements (`GUI-###`) |

Solver requirements live at [../../../requirements/opensd-solver.md](../../../requirements/opensd-solver.md).

## Conventions

Same as the repo root [requirements README](../../../requirements/README.md): **Must** / **Should**, stable IDs, acceptance criteria, implementation pointers.

## Maintenance

When changing graph layout, edges, or import behavior, update the matching `GUI-###` entry and the traceability table in `opensd-web-gui.md`.

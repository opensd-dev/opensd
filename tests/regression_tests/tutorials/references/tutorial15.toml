# OpenSD tutorial regression benchmark data.
# This file is test data, not executable code. Keep metadata with values so the source, units, and tolerance are reviewable.
# For time_checks, either use parallel times/expected arrays or [[time_checks.points]] rows.
# Point rows may include enabled = false and reason = "..." to preserve values that are intentionally excluded.
format_version = 1

[benchmark]
id = "tutorial15"
source = "PINET-tests test_des.py::test_case15 designer reference values."
independent_variable = "designer parameter"
independent_units = "dimensionless"

[[design_checks]]
quantity = "designed pipe2 loss coefficient"
native_units = "dimensionless"
file = "design_result.json"
path = "parameters.pipe2.Kforward"
expected = [
    20.2629261,
]
decimal = 4
comparison_units = "dimensionless"

[[design_checks]]
quantity = "designed pipe3 loss coefficient"
native_units = "dimensionless"
file = "design_result.json"
path = "parameters.pipe3.Kforward"
expected = [
    40.52585025,
]
decimal = 4
comparison_units = "dimensionless"
